function mostrar() {
	/*	/*  Math.random() * (max - min) + min; */
	let resultado;
	
	resultado = Math.random() * (10 - 1) + 1;
	resultado = Math.floor(resultado);
	// Para que nos de entero y no con coma usaremos el math.floor
	alert(resultado);
	
}//FIN DE LA FUNCIÓN
